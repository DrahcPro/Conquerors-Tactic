info:
  id: "<id>"
  name: "<name>"
  emoji: ":white_large_square:" #set default to square so we can see if there is an error
stats:
  range: 1
  #attack range (in tiles) upgradeable (in a square, so dx==1, dy==1 or dx & dy == 1)
character:
  lives: 3
  #out of 3, can't be bought extra
  tokens: 3
  #spending power
gameinfo:
  x-location: 0
  y-location: 0